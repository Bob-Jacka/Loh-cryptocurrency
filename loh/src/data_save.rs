use crate::base_types::SynBlock;

/**
Load blockchain
*/
pub struct BlockchainLoader {
    blocks: SynBlock,
}

/**
Entity for saving blockchain transaction
*/
pub struct BlockchainSaver {
    //
}

impl BlockchainLoader {
    pub fn new() -> BlockchainLoader {}
}

impl BlockchainSaver {}
